<%=form_with model: @task  do |f|%>
  <div>
  <%= f.label :title %>
  <%= f.text_field :title %>
  </div>

  <div>
  <%= f.label :status %>
  <%= f.select :status, options_for_select([['Todo', 'todo'], ['Doing', 'doing'], ['Done', 'done']], @task.status) %>
  </div>

  <%= f.submit "save"%>
<%end%>